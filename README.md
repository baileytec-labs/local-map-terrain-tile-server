# local-map-terrain-tile-server
This project lets a user deploy a map, terrain, and 3d surface service much like what google maps/earth provides so that they can host it locally in a containerized format instead of relying on a third party service.
